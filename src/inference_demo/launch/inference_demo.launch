<?xml version="1.0"?>
<launch>
  <node name="inference_demo_node" pkg="inference_demo" type="inference_demo_node" output="screen">
    <!-- 可选：指定配置文件路径，默认使用 config_example.yaml -->
    <!-- <param name="config_file" value="$(find inference_demo)/config_example.yaml"/> -->
    
    <!-- 可选：覆盖模型类型 -->
    <param name="model_type" value="pi_plus"/>
  </node>
</launch>
